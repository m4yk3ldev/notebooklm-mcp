# Tool Validation: `notebook_rename`
Goal: Change the title of an existing notebook.
## Test Case 1: Rename
- Command: `tools/call notebook_rename {"notebook_id": "ID", "title": "New Title"}`
- Expected: Success message.