# Tool Validation: `chat_configure`
Goal: Set response length and goals for the chat session.
## Test Case 1: Configure
- Command: `tools/call chat_configure {"notebook_id": "ID", "response_length": "longer"}`
- Expected: Success message.